diff --git a/src/hid/hid-device.cpp b/src/hid/hid-device.cpp
index e73bbc762..6ff071783 100644
--- a/src/hid/hid-device.cpp
+++ b/src/hid/hid-device.cpp
@@ -331,10 +331,13 @@ namespace librealsense
             if(fps > 0)
                 featureReport.report = (1000 / fps);
 
-            //we want to change the sensitivity values only in gyro, for FW version >= 5.16
+            // The RSUSB feature report stores gyro sensitivity in 0.1
+            // units. Preserve 0.1/0.2/... instead of truncating them to 0
+            // when encoding the unsigned on-wire field.
             if( featureReport.reportId == REPORT_ID_GYROMETER_3D
                 && _realsense_hid_report_actual_size == sizeof( REALSENSE_HID_REPORT ) )
-                featureReport.sensitivity = static_cast<unsigned short>(sensitivity);
+                featureReport.sensitivity
+                    = static_cast<unsigned short>( sensitivity * 10.0 + 0.5 );
 
 
             res = dev->control_transfer(USB_REQUEST_CODE_SET,
diff --git a/src/libusb/context-libusb.cpp b/src/libusb/context-libusb.cpp
index e8ea8be83..3b1fdc47b 100644
--- a/src/libusb/context-libusb.cpp
+++ b/src/libusb/context-libusb.cpp
@@ -13,9 +13,20 @@ namespace librealsense
             auto sts = libusb_init(&_ctx);
             if(sts != LIBUSB_SUCCESS)
             {
-                LOG_ERROR("libusb_init failed");
+                throw std::runtime_error(
+                    std::string("libusb_init failed: ")
+                    + libusb_error_name(sts));
             }
-            _count = libusb_get_device_list(_ctx, &_list);
+            const auto count = libusb_get_device_list(_ctx, &_list);
+            if(count < 0)
+            {
+                libusb_exit(_ctx);
+                _ctx = NULL;
+                throw std::runtime_error(
+                    std::string("libusb_get_device_list failed: ")
+                    + libusb_error_name(static_cast<int>(count)));
+            }
+            _count = static_cast<size_t>(count);
         }
         
         usb_context::~usb_context()
